# Copy this file to .env and adjust. docker compose reads .env automatically.
#   cp env.example .env      (macOS / Linux)
#   copy env.example .env    (Windows CMD)

MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=password123

RAW_BUCKET=nyc-taxi-raw
ICEBERG_BUCKET=nyc-taxi-iceberg

NESSIE_NAMESPACE=nyc_taxi
ICEBERG_TABLE=yellow_tripdata_2025_01

CLICKHOUSE_PASSWORD=clickhouse123
CLICKHOUSE_DATABASE=default

JUPYTER_TOKEN=corte1

# Folder assigned to the group inside the shared Azure container.
# Replace GRUPO_X with the real value before running 03_minio_to_azure.py.
GROUP_FOLDER=GRUPO_X
